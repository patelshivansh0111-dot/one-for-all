import { ComingSoon } from "@/components/shared/ComingSoon";

export default function LegacyPostPage() {
  return (
    <ComingSoon
      title="Posts"
      description="The MVP uses Questions instead of social posts. Ask something the community can help with."
    />
  );
}
